name: Run OpenCode
description: Restore OpenCode auth.json, install a pinned CLI, and run a GitHub agent or slash command.

inputs:
  model:
    description: Provider/model id (e.g., anthropic/claude-sonnet-5, openai/gpt-5.5)
    required: true
  variant:
    description: Model variant for reasoning effort / thinking budget (e.g., max, xhigh)
    required: true
  opencode-auth-json:
    description: Single-provider CI credential published from ~/.local/share/opencode/auth.json
    required: true
  codex-web-search-auth-json:
    description: OpenAI credential for the codex_web_search plugin tool; empty disables that tool
    required: false
    default: ""
  opencode-version:
    description: Optional exact OpenCode CLI version override; empty uses the pinned default
    required: false
    default: ""
  timeout-minutes:
    description: Calling job timeout; the OAuth lease must outlive it by 10 minutes
    required: true
  github-token:
    description: Write-scoped token used to publish comments and reviews
    required: true
  review-token:
    description: Read-only token used to collect pull request review context
    required: true
  prompt:
    description: Final prompt passed to opencode github run.
    required: false
    default: ""
  command:
    description: Repository-owned review command passed to opencode run --command.
    required: false
    default: ""
  command-arguments:
    description: Arguments passed to the slash command.
    required: false
    default: ""
  review-directory:
    description: Credential-free pull request checkout used as review data.
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Validate OpenCode invocation
      shell: bash
      env:
        PROMPT: ${{ inputs.prompt }}
        COMMAND: ${{ inputs.command }}
        REVIEW_DIRECTORY: ${{ inputs.review-directory }}
      run: |
        set -euo pipefail
        if [ -n "$PROMPT" ] && [ -n "$COMMAND" ]; then
          echo "::error::prompt and command are mutually exclusive."
          exit 1
        fi
        if [ -z "$PROMPT" ] && [ -z "$COMMAND" ]; then
          echo "::error::either prompt or command is required."
          exit 1
        fi
        if [ -n "$COMMAND" ] && [ "$COMMAND" != "pr-review" ]; then
          echo "::error::unsupported slash command '$COMMAND'."
          exit 1
        fi
        if [ -n "$COMMAND" ] && [ "$GITHUB_EVENT_NAME" != "pull_request_target" ]; then
          echo "::error::Review commands require a trusted pull_request_target workflow."
          exit 1
        fi
        if [ -n "$COMMAND" ] && [ "$REVIEW_DIRECTORY" != ".opencode-review-target" ]; then
          echo "::error::Review commands require the isolated .opencode-review-target checkout."
          exit 1
        fi
        if [ -n "$COMMAND" ]; then
          BASE_SHA=$(jq -r '.pull_request.base.sha // empty' "$GITHUB_EVENT_PATH")
          HEAD_SHA=$(jq -r '.pull_request.head.sha // empty' "$GITHUB_EVENT_PATH")
          if [ -z "$BASE_SHA" ] || [ -z "$HEAD_SHA" ]; then
            echo "::error::Review commands require pull request base and head commits."
            exit 1
          fi
          if [ -z "${GITHUB_WORKFLOW_SHA:-}" ]; then
            echo "::error::Review commands require the trusted workflow commit."
            exit 1
          fi
          if [ "$(git rev-parse HEAD)" != "$GITHUB_WORKFLOW_SHA" ]; then
            echo "::error::The workflow checkout does not match the trusted workflow commit."
            exit 1
          fi
          if [ "$(git -C "$REVIEW_DIRECTORY" rev-parse HEAD)" != "$HEAD_SHA" ]; then
            echo "::error::The review checkout does not match the pull request head commit."
            exit 1
          fi
        fi

    - name: Set up OpenCode
      uses: ./.github/actions/setup-opencode
      with:
        model: ${{ inputs.model }}
        variant: ${{ inputs.variant }}
        command: ${{ inputs.command }}
        opencode-auth-json: ${{ inputs.opencode-auth-json }}
        codex-web-search-auth-json: ${{ inputs.codex-web-search-auth-json }}
        timeout-minutes: ${{ inputs.timeout-minutes }}
        opencode-version: ${{ inputs.opencode-version }}

    - name: Run OpenCode command
      if: inputs.command != ''
      shell: bash
      env:
        MODEL: ${{ inputs.model }}
        VARIANT: ${{ inputs.variant }}
        COMMAND: ${{ inputs.command }}
        COMMAND_ARGUMENTS: ${{ inputs.command-arguments }}
        REVIEW_DIRECTORY: ${{ inputs.review-directory }}
        GH_TOKEN: ${{ inputs.review-token }}
        OPENCODE_DISABLE_CLAUDE_CODE: "1"
        OPENCODE_DISABLE_EXTERNAL_SKILLS: "1"
        OPENCODE_DISABLE_PROJECT_CONFIG: "1"
      run: |
        set -euo pipefail
        if ! [[ "$COMMAND_ARGUMENTS" =~ ^[1-9][0-9]*$ ]]; then
          echo "::error::$COMMAND requires one pull request number, got '$COMMAND_ARGUMENTS'."
          exit 1
        fi

        source "$GITHUB_ACTION_PATH/prefetch-context.sh" "$COMMAND_ARGUMENTS"

        # This step only ever runs for the pr-review command (validated above), which
        # setup-opencode's trusted-command step installs alongside exactly the code-review
        # skill, so the skill permission is scoped to that one allowlisted skill directly
        # rather than left on the implicit allow-all default every other unlisted permission
        # type gets, the same way bash is scoped to exact commands below.
        OPENCODE_PERMISSION=$(jq -cn --arg pr "$COMMAND_ARGUMENTS" '
          {
            edit: "deny",
            external_directory: "deny",
            bash: {
              "*": "deny",
              ("gh pr view " + $pr): "allow",
              ("gh pr diff " + $pr): "allow"
            },
            question: "deny",
            todowrite: "deny",
            skill: {"*": "deny", "code-review": "allow"}
          }
        ')
        export OPENCODE_PERMISSION

        git config --local --unset-all http.https://github.com/.extraheader 2>/dev/null || true
        unset GH_TOKEN
        cd "$REVIEW_DIRECTORY"

        COMMAND_STATUS=0
        opencode run \
          --command "$COMMAND" \
          --model "$MODEL" \
          --variant "$VARIANT" \
          --format json \
          "$COMMAND_ARGUMENTS" \
          | tee "$RUNNER_TEMP/opencode-command.ndjson" \
          || COMMAND_STATUS=$?

        if [ "$COMMAND_STATUS" -eq 0 ]; then
          jq -jrs \
            --arg operation "final-text" \
            --arg command "$COMMAND" \
            -f "$GITHUB_WORKSPACE/.github/actions/setup-opencode/extract-result.jq" \
            "$RUNNER_TEMP/opencode-command.ndjson" \
            > "$RUNNER_TEMP/opencode-command-result.md"
          exit 0
        fi

        # Review commands delegate to a task, then ask the parent
        # model to present that result. Preserve the completed review when only
        # that final turn fails with a retryable provider error.
        if jq -jers \
          --arg operation "completed-review" \
          --arg command "$COMMAND" \
          -f "$GITHUB_WORKSPACE/.github/actions/setup-opencode/extract-result.jq" \
          "$RUNNER_TEMP/opencode-command.ndjson" \
          > "$RUNNER_TEMP/opencode-command-result.md"; then
          echo "::notice::Publishing the completed review task after a retryable final provider error."
          exit 0
        fi

        exit "$COMMAND_STATUS"

    - name: Publish OpenCode review
      if: inputs.command != ''
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.github-token }}
        MODEL: ${{ inputs.model }}
        VARIANT: ${{ inputs.variant }}
        OPENCODE_VERSION: ${{ env.OPENCODE_VERSION }}
      run: |
        set -euo pipefail
        PR_NUMBER=$(jq -r '.pull_request.number' "$GITHUB_EVENT_PATH")
        HEAD_SHA=$(jq -r '.pull_request.head.sha' "$GITHUB_EVENT_PATH")
        BASE_SHA=$(jq -r '.pull_request.base.sha' "$GITHUB_EVENT_PATH")
        read -r CURRENT_HEAD CURRENT_BASE < <(
          gh api "repos/$GITHUB_REPOSITORY/pulls/$PR_NUMBER" --jq '[.head.sha, .base.sha] | @tsv'
        )
        if [ "$CURRENT_HEAD" != "$HEAD_SHA" ] || [ "$CURRENT_BASE" != "$BASE_SHA" ]; then
          echo "::notice::Skipping stale review for base/head $BASE_SHA/$HEAD_SHA; pull request now points to $CURRENT_BASE/$CURRENT_HEAD."
          exit 0
        fi

        jq -n \
          --arg head "$HEAD_SHA" \
          --arg model "$MODEL" \
          --arg variant "$VARIANT" \
          --arg opencode_version "$OPENCODE_VERSION" \
          --arg run_url "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" \
          --arg commit_url "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/commit/$HEAD_SHA" \
          --rawfile review "$RUNNER_TEMP/opencode-command-result.md" \
          -f "$GITHUB_ACTION_PATH/review-payload.jq" \
          > "$RUNNER_TEMP/github-review.json"

        gh api \
          --method POST \
          "repos/$GITHUB_REPOSITORY/pulls/$PR_NUMBER/reviews" \
          --input "$RUNNER_TEMP/github-review.json"

    # Mirrors the wrapper action's env-driven entrypoint. use_github_token=true
    # makes opencode skip OIDC and use GITHUB_TOKEN for the GitHub API; provider
    # (model) auth comes from the auth.json restored above.
    - name: Run OpenCode GitHub agent
      if: inputs.command == ''
      shell: bash
      env:
        MODEL: ${{ inputs.model }}
        VARIANT: ${{ inputs.variant }}
        PROMPT: ${{ inputs.prompt }}
        USE_GITHUB_TOKEN: "true"
        GITHUB_TOKEN: ${{ inputs.github-token }}
      run: opencode github run
