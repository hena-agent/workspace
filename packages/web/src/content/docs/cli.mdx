---
title: CLI
description: Hena Agent CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Hena Agent CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
hena-agent
```

But it also accepts commands as documented on this page. This allows you to interact with Hena Agent programmatically.

```bash
hena-agent run "Explain how closures work in JavaScript"
```

---

### tui

Start the Hena Agent terminal user interface.

```bash
hena-agent [project]
```

#### Flags

| Flag                                        | Short | Description                                                             |
| ------------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr>    | `-c`  | Continue the last session                                               |
| <nobr><code>{"--session"}</code></nobr>     | `-s`  | Session ID to continue                                                  |
| <nobr><code>{"--fork"}</code></nobr>        |       | Fork the session when continuing (use with `--continue` or `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>      |       | Prompt to use                                                           |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use in the form of provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>       |       | Agent to use                                                            |
| <nobr><code>{"--auto"}</code></nobr>        |       | Auto-approve permissions that are not explicitly denied                 |
| <nobr><code>{"--port"}</code></nobr>        |       | Port to listen on                                                       |
| <nobr><code>{"--hostname"}</code></nobr>    |       | Hostname to listen on                                                   |
| <nobr><code>{"--mdns"}</code></nobr>        |       | Enable mDNS discovery                                                   |
| <nobr><code>{"--mdns-domain"}</code></nobr> |       | Custom mDNS domain name                                                 |
| <nobr><code>{"--cors"}</code></nobr>        |       | Additional browser origin(s) to allow CORS                              |

---

## Commands

The Hena Agent CLI also has the following commands.

---

### agent

Manage agents for Hena Agent.

```bash
hena-agent agent [command]
```

---

#### create

Create a new agent with custom configuration.

```bash
hena-agent agent create
```

This command will guide you through creating a new agent with a custom system prompt and permission configuration. Anything you don't allow is denied in the generated agent's frontmatter.

#### Flags

| Flag                                        | Short | Description                                                                                                                                                                                                                |
| ------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--path"}</code></nobr>        |       | Directory to write the agent file to (defaults to global or `.hena-agent/agent` based on the prompt)                                                                                                                         |
| <nobr><code>{"--description"}</code></nobr> |       | What the agent should do                                                                                                                                                                                                   |
| <nobr><code>{"--mode"}</code></nobr>        |       | Agent mode: `all`, `primary`, or `subagent`                                                                                                                                                                                |
| <nobr><code>{"--permissions"}</code></nobr> |       | Comma-separated list of permissions to allow (default: all). Available: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Anything omitted is denied. Alias: `--tools` |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use, in `provider/model` format                                                                                                                                                                                   |

Passing all of `--path`, `--description`, `--mode`, and `--permissions` runs the command non-interactively.

---

#### list

List all available agents.

```bash
hena-agent agent list
```

---

### attach

Attach a terminal to an already running Hena Agent backend server started via `serve` or `web` commands.

```bash
hena-agent attach [url]
```

This allows using the TUI with a remote Hena Agent backend. For example:

```bash
# Start the backend server for web/mobile access
hena-agent web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
hena-agent attach http://10.20.30.40:4096
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory to start TUI in                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `HENA_AGENT_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `HENA_AGENT_SERVER_USERNAME` or `hena-agent`) |

---

### auth

Command to manage credentials and login for providers.

```bash
hena-agent auth [command]
```

---

#### login

Hena Agent is powered by the provider list at [Models.dev](https://models.dev), so you can use `hena-agent auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/hena-agent/auth.json`.

```bash
hena-agent auth login
```

When Hena Agent starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

##### Flags

| Flag                                     | Short | Description                                          |
| ---------------------------------------- | ----- | ---------------------------------------------------- |
| <nobr><code>{"--provider"}</code></nobr> | `-p`  | Provider ID or name to log in to                     |
| <nobr><code>{"--method"}</code></nobr>   | `-m`  | Login method label to use, skipping method selection |

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
hena-agent auth list
```

Or the short version.

```bash
hena-agent auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
hena-agent auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
hena-agent github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
hena-agent github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
hena-agent github run
```

##### Flags

| Flag                                  | Description                            |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event to run the agent for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
hena-agent mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
hena-agent mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
hena-agent mcp list
```

Or use the short version.

```bash
hena-agent mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
hena-agent mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
hena-agent mcp auth list
```

Or use the short version.

```bash
hena-agent mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
hena-agent mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
hena-agent mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
hena-agent models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
hena-agent models anthropic
```

#### Flags

| Flag                                    | Description                                                  |
| --------------------------------------- | ------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Refresh the models cache from models.dev                     |
| <nobr><code>{"--verbose"}</code></nobr> | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Hena Agent.

```bash
hena-agent models --refresh
```

---

### run

Run hena-agent in non-interactive mode by passing a prompt directly.

```bash
hena-agent run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "hena-agent run"
hena-agent run Explain the use of context in Go
```

You can also attach to a running `hena-agent serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
hena-agent serve

# In another terminal, run commands that attach to it
hena-agent run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | The command to run, use message for args                                   |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--share"}</code></nobr>    |       | Share the session                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Model to use in the form of provider/model                                 |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agent to use                                                               |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | File(s) to attach to message                                               |
| <nobr><code>{"--format"}</code></nobr>   |       | Format: default (formatted) or json (raw JSON events)                      |
| <nobr><code>{"--title"}</code></nobr>    |       | Title for the session (uses truncated prompt if no value provided)         |
| <nobr><code>{"--attach"}</code></nobr>   |       | Attach to a running hena-agent server (e.g., http://localhost:4096)          |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `HENA_AGENT_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `HENA_AGENT_SERVER_USERNAME` or `hena-agent`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directory to run in, or path on the remote server when attaching           |
| <nobr><code>{"--port"}</code></nobr>     |       | Port for the local server (defaults to random port)                        |
| <nobr><code>{"--variant"}</code></nobr>  |       | Model variant (provider-specific reasoning effort)                         |
| <nobr><code>{"--thinking"}</code></nobr> |       | Show thinking blocks                                                       |
| <nobr><code>{"--auto"}</code></nobr>     |       | Auto-approve permissions that are not explicitly denied                    |

---

### serve

Start a headless Hena Agent server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
hena-agent serve
```

This starts an HTTP server that provides API access to hena-agent functionality without the TUI interface. Set `HENA_AGENT_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `hena-agent`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### session

Manage Hena Agent sessions.

```bash
hena-agent session [command]
```

---

#### list

List all Hena Agent sessions.

```bash
hena-agent session list
```

##### Flags

| Flag                                      | Short | Description                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limit to N most recent sessions      |
| <nobr><code>{"--format"}</code></nobr>    |       | Output format: table or json (table) |

---

#### delete

Delete an Hena Agent session.

```bash
hena-agent session delete <sessionID>
```

---

### stats

Show token usage and cost statistics for your Hena Agent sessions.

```bash
hena-agent stats
```

#### Flags

| Flag                                    | Description                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Show stats for the last N days (all time)                                   |
| <nobr><code>{"--tools"}</code></nobr>   | Number of tools to show (all)                                               |
| <nobr><code>{"--models"}</code></nobr>  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| <nobr><code>{"--project"}</code></nobr> | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
hena-agent export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

#### Flags

| Flag                                     | Description                           |
| ---------------------------------------- | ------------------------------------- |
| <nobr><code>{"--sanitize"}</code></nobr> | Redact sensitive transcript/file data |

---

### import

Import session data from a JSON file or Hena Agent share URL.

```bash
hena-agent import <file>
```

You can import from a local file or an Hena Agent share URL.

```bash
hena-agent import session.json
hena-agent import https://hena.dev/s/abc123
```

---

### web

Start a headless Hena Agent server with a web interface.

```bash
hena-agent web
```

This starts an HTTP server and opens a web browser to access Hena Agent through a web interface. Set `HENA_AGENT_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `hena-agent`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
hena-agent acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr>         | Working directory                          |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### plugin

Install a plugin and update your config.

```bash
hena-agent plugin <module>
```

Or use the alias.

```bash
hena-agent plug <module>
```

#### Flags

| Flag                                   | Short | Description                     |
| -------------------------------------- | ----- | ------------------------------- |
| <nobr><code>{"--global"}</code></nobr> | `-g`  | Install in global config        |
| <nobr><code>{"--force"}</code></nobr>  | `-f`  | Replace existing plugin version |

---

### pr

Fetch and checkout a GitHub PR branch, then run Hena Agent.

```bash
hena-agent pr <number>
```

---

### db

Database tools.

```bash
hena-agent db [query]
```

#### Flags

| Flag                                   | Description                    |
| -------------------------------------- | ------------------------------ |
| <nobr><code>{"--format"}</code></nobr> | Output format: `json` or `tsv` |

---

#### path

Print the database path.

```bash
hena-agent db path
```

---

### debug

Debugging and troubleshooting tools.

```bash
hena-agent debug [command]
```

---

### uninstall

Uninstall Hena Agent and remove all related files.

```bash
hena-agent uninstall
```

#### Flags

| Flag                                        | Short | Description                                 |
| ------------------------------------------- | ----- | ------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Keep configuration files                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Keep session data and snapshots             |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Show what would be removed without removing |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates hena-agent to the latest version or a specific version.

```bash
hena-agent upgrade [target]
```

To upgrade to the latest version.

```bash
hena-agent upgrade
```

To upgrade to a specific version.

```bash
hena-agent upgrade v0.1.48
```

#### Flags

| Flag                                   | Short | Description                                                       |
| -------------------------------------- | ----- | ----------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The hena-agent CLI takes the following global flags.

| Flag                                       | Short | Description                          |
| ------------------------------------------ | ----- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Display help                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Print version number                 |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Print logs to stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Log level (DEBUG, INFO, WARN, ERROR) |
| <nobr><code>{"--pure"}</code></nobr>       |       | Run without external plugins         |

---

## Environment variables

Hena Agent can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `HENA_AGENT_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `HENA_AGENT_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `HENA_AGENT_CONFIG`                     | string  | Path to config file                               |
| `HENA_AGENT_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `HENA_AGENT_CONFIG_DIR`                 | string  | Path to config directory                          |
| `HENA_AGENT_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `HENA_AGENT_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `HENA_AGENT_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `HENA_AGENT_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `HENA_AGENT_PERMISSION`                 | string  | Inlined json permissions config                   |
| `HENA_AGENT_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `HENA_AGENT_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `HENA_AGENT_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `HENA_AGENT_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `HENA_AGENT_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `HENA_AGENT_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `HENA_AGENT_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `HENA_AGENT_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `HENA_AGENT_DISABLE_MOUSE`              | boolean | Disable mouse capture in the TUI                  |
| `HENA_AGENT_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `HENA_AGENT_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `HENA_AGENT_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `HENA_AGENT_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `HENA_AGENT_SERVER_USERNAME`            | string  | Override basic auth username (default `hena-agent`) |
| `HENA_AGENT_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `HENA_AGENT_EXPERIMENTAL`                         | boolean | Enable the experimental umbrella flag   |
| `HENA_AGENT_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `HENA_AGENT_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `HENA_AGENT_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `HENA_AGENT_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `HENA_AGENT_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `HENA_AGENT_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `HENA_AGENT_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `HENA_AGENT_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `HENA_AGENT_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `HENA_AGENT_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `HENA_AGENT_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
| `HENA_AGENT_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Enable background subagent tasks        |
| `HENA_AGENT_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Enable experimental event system        |
| `HENA_AGENT_EXPERIMENTAL_NATIVE_LLM`              | boolean | Enable native LLM request path          |
| `HENA_AGENT_EXPERIMENTAL_PARALLEL`                | boolean | Enable parallel web search execution    |
| `HENA_AGENT_EXPERIMENTAL_SCOUT`                   | boolean | Enable Scout subagent                   |
| `HENA_AGENT_EXPERIMENTAL_WORKSPACES`              | boolean | Enable workspace support                |
