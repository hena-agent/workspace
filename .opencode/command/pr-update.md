---
description: Address worthwhile PR feedback, fix CI failures, and push updates.
agent: build
---

Read all the new PR comments, accept only the reviews worth accepting, and update the PR by pushing the changes.

Don't accept every code review unconditionally — evaluate them critically.

Also fix CI Actions if there are any failures.
