---
title: Web
description: En utilisant Hena Agent dans votre navigateur.
---

Hena Agent peut s'exécuter comme une application Web dans votre navigateur, offrant la même expérience puissante de codage d'IA sans avoir besoin d'un terminal.

![Hena Agent Web - Nouvelle session](../../../assets/web/web-homepage-new-session.png)

## Commencer

Démarrez l'interface Web en exécutant :

```bash
hena-agent web
```

Cela démarre un serveur local sur `127.0.0.1` avec un port disponible aléatoire et ouvre automatiquement Hena Agent dans votre navigateur par défaut.

:::caution
Si `HENA_AGENT_SERVER_PASSWORD` n'est pas défini, le serveur ne sera pas sécurisé. C'est bien pour une utilisation locale mais doit être défini pour l'accès au réseau.
:::

:::tip[Windows Utilisateurs]
Pour une expérience optimale, exécutez `hena-agent web` à partir de [WSL](/docs/windows-wsl) plutôt que de PowerShell. Cela garantit un accès correct au système de fichiers et une intégration du terminal.
:::

---

## Configuration

Vous pouvez configurer le serveur Web à l'aide d'indicateurs de ligne de commande ou dans votre [fichier de configuration](/docs/config).

### Port

Par défaut, Hena Agent sélectionne un port disponible. Vous pouvez spécifier un port :

```bash
hena-agent web --port 4096
```

### Nom d'hôte

Par défaut, le serveur se lie à `127.0.0.1` (localhost uniquement). Pour rendre Hena Agent accessible sur votre réseau :

```bash
hena-agent web --hostname 0.0.0.0
```

Lorsque vous utilisez `0.0.0.0`, Hena Agent affichera les adresses locales et réseau :

```
  Local access:       http://localhost:4096
  Network access:     http://192.168.1.100:4096
```

### Découverte mDNS

Activez mDNS pour rendre votre serveur visible sur le réseau local :

```bash
hena-agent web --mdns
```

Cela définit automatiquement le nom d'hôte sur `0.0.0.0` et annonce le serveur comme `hena-agent.local`.

Vous pouvez personnaliser le nom de domaine mDNS pour exécuter plusieurs instances sur le même réseau :

```bash
hena-agent web --mdns --mdns-domain myproject.local
```

### CORS

Pour autoriser des domaines supplémentaires pour CORS (utile pour les frontends personnalisés) :

```bash
hena-agent web --cors https://example.com
```

### Authentification

Pour protéger l'accès, définissez un mot de passe à l'aide de la variable d'environnement `HENA_AGENT_SERVER_PASSWORD` :

```bash
HENA_AGENT_SERVER_PASSWORD=secret hena-agent web
```

Le nom d'utilisateur par défaut est `hena-agent` mais peut être modifié avec `HENA_AGENT_SERVER_USERNAME`.

---

## Utilisation de l'interface Web

Une fois démarrée, l'interface web permet d'accéder à vos sessions Hena Agent.

### Sessions

Consultez et gérez vos sessions depuis la page d'accueil. Vous pouvez voir les sessions actives et en démarrer de nouvelles.

![Hena Agent Web - Session active](../../../assets/web/web-homepage-active-session.png)

### Statut du serveur

Cliquez sur "Voir les serveurs" pour afficher les serveurs connectés et leur état.

![Hena Agent Web - Voir Serveurs](../../../assets/web/web-homepage-see-servers.png)

---

## Attacher un terminal

Vous pouvez connecter un terminal TUI à un serveur Web en cours d'exécution :

```bash
# Start the web server
hena-agent web --port 4096

# In another terminal, attach the TUI
hena-agent attach http://localhost:4096
```

Cela vous permet d'utiliser simultanément l'interface Web et le terminal, en partageant les mêmes sessions et le même état.

---

## Fichier de configuration

Vous pouvez également configurer les paramètres du serveur dans votre fichier de configuration `hena-agent.json` :

```json
{
  "server": {
    "port": 4096,
    "hostname": "0.0.0.0",
    "mdns": true,
    "cors": ["https://example.com"]
  }
}
```

Les indicateurs de ligne de commande ont priorité sur les paramètres du fichier de configuration.
