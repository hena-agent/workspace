name: typecheck

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]
  workflow_dispatch:

jobs:
  typecheck:
    if: ${{ !contains(github.event.pull_request.title, '[skip ci]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      # ubuntu-latest gives 2 vCPU / 7 GB on this plan. Turbo's default
      # concurrency starts enough parallel tsgo processes to exhaust that and
      # get the runner OOM-killed ("received a shutdown signal") partway
      # through @opencode-ai/ui, so cap it.
      - name: Run typecheck
        run: bun turbo typecheck --concurrency=1
